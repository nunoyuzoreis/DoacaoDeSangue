<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="nunoreis.doacaodesangue.RegistrarFuncionario">


    <TextView
        android:id="@+id/txt_registrar_funcionario"
        android:text="@string/registrar_funcionario"
        android:textColor="@color/black_semi_transparent"
        android:layout_width="fill_parent"
        android:gravity="center"
        android:layout_height="wrap_content" />

    <EditText
        android:id="@+id/login_funcionario"
        android:hint="@string/prompt_userName"
        android:maxLines="1"
        android:singleLine="true"
        android:layout_marginTop="35dp"
        android:layout_below="@id/txt_registrar_funcionario"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:imeOptions="actionNext"/>

    <EditText
        android:id="@+id/senha_funcionario"
        android:hint="@string/prompt_password"
        android:imeOptions="actionDone"
        android:inputType="textPassword"
        android:maxLines="1"
        android:singleLine="true"
        android:layout_below="@id/login_funcionario"
        android:layout_marginTop="15dp"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content" />

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/senha_funcionario"
        android:layout_marginTop="15dp">

        <Button
            android:layout_weight="0.50"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:id="@+id/botao_cancelar_funcionario"
            android:text="@string/botao_cancelar"/>
        <Button
            android:layout_weight="0.50"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:id="@+id/botao_registrar_funcionario"
            android:text="@string/botao_registrar"
            android:layout_toEndOf="@+id/botao_cancelar_funcionario"/>

    </LinearLayout>




</RelativeLayout>
